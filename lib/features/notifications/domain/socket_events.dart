/// Socket.IO event names emitted by the backend.
abstract final class SocketEvents {
  SocketEvents._();

  static const String notification = 'notification';
  static const String botResponse = 'bot_response';
}
